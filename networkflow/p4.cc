#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin >> n;
    vector<int> a(n);
    stack<pair<int,int>> st;
    for (int i=0;i<n;i++) cin >> a[i];
    st.push(make_pair(a[0],1));
    for (int i=1;i<n;i++) {
        
    }
    
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}